query listDatasetsAndAnalysis {
    allDatasets(orderBy: "name") {
        results {
            id
            name
            spectrogramAnalysis(orderBy: "name") {
                results {
                    id
                    name
                    colormap {
                        name
                    }
                }
            }
        }
    }
}
