
mutation updateCurrentUserPassword(
    $oldPassword: String!
    $newPassword: String!
) {
    userUpdatePassword(
        input: {
            oldPassword: $oldPassword
            newPassword: $newPassword
        }
    ) {
        errors {
            field
            messages
        }
    }
}
