fragment Label on AnnotationLabelNode { id, name }

query listLabelSets {
    allLabelSets {
        results {
            id
            name
            description
            labels { ...Label }
        }
    }
}
