fragment AnnotationLabel on AnnotationLabelNode { id, name }

query listLabelSets {
    allLabelSets {
        results {
            id
            name
            description
            labels { ...AnnotationLabel }
        }
    }
}
