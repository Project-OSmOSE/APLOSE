fragment ProjectCard on ProjectNodeOverride {
    id, name,
    projectType { name }
    startDate, endDate
    accessibility
}

query allProjectCards {
    allProjects { results { ...ProjectCard } }
}