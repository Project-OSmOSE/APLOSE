query allBackgroundTasks {
    allImportAnalysisTasks {
        identifier
        status
        error
        errorTrace
        completionPercentage
        analysisPath
    }

    _debug {
        exceptions {
            stack
        }
    }
}