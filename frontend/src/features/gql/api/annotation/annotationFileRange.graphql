query getFileRangeDates(
    $campaignID: ID!,
    $annotatorID: ID!,
    $phase: AnnotationPhaseType!,
) {
    allAnnotationFileRanges(
        annotationCampaignId: $campaignID
        annotatorId: $annotatorID
        phaseType: $phase
    ) {
        results {
            fromDatetime
            toDatetime
        }
    }
}