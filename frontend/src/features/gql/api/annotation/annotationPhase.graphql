query getPhase(
    $campaignID: PK!
    $phaseType: AnnotationPhaseType!
) {
    annotationPhaseForCampaign(
        campaignId: $campaignID,
        phaseType: $phaseType
    ) {
        annotationCampaign {
            archive {
                date
            }
        }
        endedAt
        phase
        hasAnnotations
    }
}